<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.LifeLog" parent="android:Theme.Material.Light.NoActionBar" />
    <!-- Transparent window for QuickAddActivity so only the bottom sheet is visible -->
    <style name="Theme.LifeLog.Transparent" parent="Theme.LifeLog">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
    <style name="Theme.LifeLog.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash</item>
        <item name="postSplashScreenTheme">@style/Theme.LifeLog</item>
    </style>
    <!-- Applied programmatically when the app's dark theme preference is enabled -->
    <style name="Theme.LifeLog.Splash.Dark" parent="Theme.LifeLog.Splash">
        <item name="windowSplashScreenBackground">#000000</item>
    </style>
</resources>
